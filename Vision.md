VISION: Aim to build a user-friendly, Quick coding & Fast game Engine.
1. User-friendly: simple to use
2. Quick coding: short code, short syntax, less functions need to call 
3. Fast: lightweight, suit for very heavy logic

# Inspired From
1. Classes in html/css/js: use string with space to list component 
2. JS: key-value: able to  get value via key-value like func("name") instead of func().name -> more flexible
3. Centralise: a function should do completly its task (S:SOLID but call once function for a task)
